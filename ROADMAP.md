# RTS Arcade — 商业化 ROADMAP

> 硬骨架（锁步 + 定点数 + 模拟线程 + 实例化渲染）已经是商业级；软骨架（网络韧性、回放、AI、CI、工程卫生）还停留在原型级。
> 优先级按对商业化的影响排序。每条都有明确的验收标准，落地顺序 P0 → P1 → P2 → P3。

## P0 — 不补就无法称“基底”

### P0-1 逻辑层迁移收尾

战斗/行为还挂在 Godot 节点上（Brain/Combat/Life 是 Node），模拟线程隔着视觉对象跑逻辑。每个新技能都要人工审计线程安全，右键丢指令、集合竞争、AccessViolation 全从这里漏出来。

**目标**：把冷却/索敌/行为状态机搬进 SimUnit；视觉节点只做镜像。

**验收**：
- 模拟线程内不再触碰 Godot 节点 API（Node 状态、Tween、Transform）。
- 冷却、索敌、行为状态全部可在 SimUnit 上序列化/哈希。
- 新增一个技能时无需人工审计线程安全。

**进度**：
- [x] 里程碑 1：战斗状态镜像——`CombatTargetId / ActiveWeaponCooldown / WeaponRotationCooldown` 写入 SimUnit 并纳入确定性哈希（`Weapon.CooldownRemainingFP`、`UnitCombat.MirrorStateToSimUnit`）。
- [x] 里程碑 2：武器冷却/交替间隔计时器本体搬进 SimUnit（`SimUnit.WeaponCooldowns / WeaponRotationCooldown`，`Weapon.CurrentCooldown` 路由；ProjectileWeapon 同步改）。
- [~] 里程碑 3：索敌状态以 `SimUnit.CombatTargetId` 为准（节点只做 ID→实体解析）；当前行为名 `ActiveActionName` 纳入哈希。行为状态机本体迁移尚未完成。
- [~] 里程碑 4：第一轮静态审计完成——模拟线程路径上 Godot API 调用最多的非视觉文件：`Rifle.cs(15)`、`HomingProjectile.cs(11)`、`ProjectileWeapon.cs(9)`、`UnitActionController.cs(5)`（缓存缺失时的延迟刷新）；视觉层 `UnitVisuals.cs(48)` 属镜像层不计入。下一步逐文件清零。
- [x] 里程碑 4 首修：`RebuildUnitActions` 重建动作树后立即在主线程刷新动作缓存，消除模拟线程“缓存缺失→丢一次指令”的窗口（右键丢指令家族问题之一）。
- [x] 里程碑 4 续修（建造/取消路径清零）：`GeneralCancelAction.OnEnter` 不再场景树遍历（改用 `_unit`）；建造完成时 `VisualsModule`/取消动作 `GetAction+QueueFree` 延迟主线程；取消/蓝图完成路径的节点 `QueueFree` 延迟到主线程派发屏障。验收：`check_sim_thread.py` PASS + Arena 600 帧确定性 PASS + matrix=16 PASS。

### P0-2 回放系统

锁步架构下回放 = 录输入 + 重放，成本最低却最该先做；回放同时是 desync 调试的最小复现工具。

**验收**：录一场 2000 单位对局，重放后 hash 逐 tick 一致。

**进度**：
- [x] 数据层：`LockstepManager` 支持回放模式（逐 tick 喂回录制的 NetAction）、录制文件读写（`SaveReplayToFile/LoadReplayFromFile`）、逐 tick 哈希记录与比对（`VerifyReplayHashes`，30 tick 校验点）。
- [x] 回放驱动：`ReplayAutoRecord/ReplayAutoVerify`（RECORD_REPLAY/REPLAY_FILE 环境变量），已挂入压力测试场景。
- [x] 验收：**2000 枪兵（1000v1000）对局录制→重放，19 个哈希逐 tick 一致（PASS）**；压力测试加 `REPLAY_DETERMINISTIC=1` 同步生成保证两次运行世界一致。
- [x] 真实游戏开局（非压力场景）的生成时序确定性：开局生成集中在 `Game.SetupMatch` 单次同步调用内（首 tick 前完成），生成源为保序数组、ID 走单一计数器、RNG 固定种子，双端顺序一致；`Game._Ready` 现按 `RECORD_REPLAY/REPLAY_FILE` 自动挂载回放工具，真实开局可直接录制→重放逐 tick 校验。验收：构建 PASS + SIM_THREAD_CHECK_PASS + Arena 600 帧确定性 PASS。

### P0-3 网络韧性

目前只有 Steam 大厅直连；没有掉线重连、主机迁移、动态输入延迟（TICK_DELAY=4 写死）、迟到包追赶策略。断线一次 = 整局报废。

**验收**：中途拔线/杀进程后，对局可恢复且 hash 一致；延迟抖动下有迟到包追赶、无卡死。

**进度**：
- [x] 动态输入延迟基础：`TICK_DELAY` 常量改为实例属性 `TickDelay`（默认 4），可随网络质量/人数调节。
- [x] 动态自适应：卡 tick（连续未就绪）自动上调 TickDelay（≤8），稳定 600 tick 后回落（≥3）。
- [x] 迟到包容忍窗口可配置（`LateTickTolerance`，默认 10）。
- [ ] 迟到包追赶完善/主机迁移/掉线重连。

### P0-4 确定性保障闭环

有双世界对拍，但覆盖不到网络/延迟闭包路径（右键 bug 就漏在这）；没有双客户端自动对拍、没有 CI、没有“每类指令/每个种族/每个技能”的确定性矩阵。

**验收**：CI 跑全种族×全技能×全指令的确定性矩阵；双客户端自动对拍纳入日常回归。

**进度**：
- [x] Arena 确定性矩阵：`ValidateCommandRaceMatrix`（种族×单位×Move/AttackMove/Attack/Stop，双世界反向指令序对拍）已并入 `SimulationSelfTest`，实测 **matrix=16 cells PASS**。
- [x] CI 脚本：`Tools/run_ci.ps1`（编译 RTSarcade + Arena 确定性自测 + 双客户端网络对拍，失败即非零退出）。
- [x] 双客户端自动对拍接入 CI：`Test-Network.ps1` 实测 **NET_PASS P1/P2 frame=200 hash 一致**。

## P1 — 影响“能不能卖”

### P1-1 对战 AI

至少要有带难度分级的对战 AI，否则单人内容为零。

**验收**：可选 3 档难度 AI，能完成基础运营、出兵、进攻。

**进度**：
- [x] 第一版：`BOT_TEAMS=1,2` 指定机器人阵营，每 30 tick 自动索敌/攻击移动；生产建筑队列空且资源够自动训练（`StartAction`）。
- [x] 确定性验证：机器人进攻局录制→重放，9/9 哈希一致 PASS。
- [x] 难度分级：`BOT_DIFFICULTY=1/2/3`（决策间隔 60/40/20 tick）。
- [x] 采集调度：空闲工人自动去最近资源点（矿点/气泉），工人不再跟随进攻冲锋；经济/军事分流。
- [x] UI 开启入口：单机大厅可选机器人数量（0-3）与难度（休闲/标准/疯狂），静态覆盖 `BOT_TEAMS/BOT_DIFFICULTY`；`NetworkManager.AddOfflineBots` 把机器人注册为离线玩家，保证其初始内容生成。
- [x] 难度改为资源倍率：0.7x / 1.0x / 1.5x 作用于全部收入点（工人交付、自动采集、建筑收入、纳米菌毯、泰伦驻扎、多足数据），决策频率固定 30 tick。
- [x] 分族 AI（数据表驱动）：`RaceConfig` 新增 `AiBuildOrderIds/AiTechOrderIds/AiUnitMixIds/AiWorkerTarget/AiWorkerUnitId/AiArmyTarget`，五个种族表已填不同计划；`SimManager.BotAI.cs` 按种族执行——联盟/恶魔/泰伦/多足走工人建造+训练+科技，纳米走菌毯扩散+凭空建造+科技，泰伦自动进驻藻类工厂产生物质；建造/扩散全部镜像人类锁步派发（确定性校验+主线程生成）。
- [x] 种族机制感知战斗：恶魔推进点钳制在己方立场覆盖内（出圈立即拉回，避免 -10 血/s）；多足只指挥控制圈内单位、推进点钳制在控制半径内（牧羊人/电浆炮控制，出圈丢控制）；泰伦架设管理（推进前先收起、到位后架设，解放者按敌方集群指定范围圈）；多足蓝图按 `AiBuildTargetCounts` 控制产出数量（牧羊人数量=人口上限）。
- [x] 泰伦弹药管理：弹药 <30% 且不在补给范围 → 自动回撤到最近弹药补给建筑（地面/飞行按 `AmmoRangeAffectsAir` 区分），补给圈内只防守不推进。
- [x] 分矿扩张：中立塔资源点（2 铁矿+1 气泉，塔 TeamID=-2 可攻击）→ 兵力 ≥3 派兵拆塔（恶魔/多足仍钳制在立场/控制安全点）→ 塔拆除后在资源簇旁按 `AiExpansionBaseIds` 拍分矿（联盟指挥中心/恶魔地狱城/泰伦堡垒核心/纳米采集器/多足收割+牧羊蓝图）；已占地（12 格内有己方建筑）不再重复进攻。
- [x] 1v1 AI 演示场景：`Scenes/Test/AIShowcase1v1.tscn`（双方机器人对战，T1/T2 种族与难度可配，OB 正交俯视全图，`FogOfWar.RevealAll` 只影响观看渲染、机器人视野/行为正常）；配套 `LockstepManager` 离线多玩家自动就绪、`SimManager.SetBotTeams` 运行期配置。
- [x] 倍速（演示/回放/离线）：`SimManager.SimulationSpeed` 1/2/4/8，放大模拟时间流速并同步放大单轮 tick 上限（屏障协议不变）；联机自动强制 1x；演示场景数字键 1/2/3/4 切换、顶部实时显示。确定性：tick 内容与顺序不变，Arena 600 帧哈希一致。
- [x] 恶魔前线立场建筑推进：地狱城怨灵满 12 后，AI 沿立场边缘向最近中立塔/敌方集群方向逐段铺 SoulStone，立场像蠕虫一样蔓延（拆塔评估 打?True 实测通过）。
- [x] 全局经济修复：工人背包容积从配置表注入（原默认 10，实为 50），全种族采集收入提升约 5 倍；非金属资源（气/野果）保底 2 工人，金属指派不再抢特殊资源。
- [x] 出兵节奏修复：早期兵力引导（先出 2 个攻击兵再补工人）、兵种轮换不再空转（买得起就继续造）、支援兵配比上限（医疗 ≤ 攻击兵一半）。
- [x] 主动进攻判定：兵力过半/战力优势 1.2x 也触发推进，不再干站到死。
- [x] 泰伦修复：AlgaeFactory 不再被科技门锁死，建造顺序改为 经济→兵营→补给→T2→T3→科技；野果可达性按主基地中心计算（4x4 堡垒角落导致野果被判 12 格外）。
- [x] 多足修复：无主基地建筑的种族以控制单位（牧羊人）为基地锚点，资源点按控制圈判定可达，蓝图的建造/采集恢复正常。
- [x] 三段式节奏：拆塔→开分矿→总攻。拆塔按波次优先（一半满编兵力 + 军力 ≥ 塔战力 1.5 倍才开拆，开拆后维持到底）；塔一倒固定拍分矿（劣势也拍，待办队列保证不丢）；兵力满编后集体打敌方基地（建筑质心而非单位质心）。
- [x] 波次系统：进攻/拆塔先在家门口集结（9 秒或兵力过半），再一起 attack-move；基地被摸跳过集结直接防守。不再一个兵一个兵送。
- [x] 恶魔寻路铺场：SoulStone 沿 A* 路径一小步一小步从立场边缘外扩（不再直线穿墙/直接跳塔）；怨灵工人豁免立场伤害，保证圈外建造可行。
- [x] 恶魔拆塔：狗没有打塔能力，必须出英雄（HellLord/FireTitan）后才拆塔。
- [x] AI 自动化回归：AIShowcase 支持 AI_TEST_SPEED/AI_TEST_TICKS/AI_TEST_RACE1/AI_TEST_RACE2 环境变量，无头跑全种族矩阵。
- [ ] 多机器人差异化策略、机器人种族克制权重。

### P1-2 真实对局性能

性能只验证了单兵种场景：2000 枪兵 120fps 达标，但混合种族、建筑、弹道、特效、菌毯的真实对局没压过；非批量单位仍是每单位几十个节点（4 万节点问题只解决了一半）。

**验收**：2000 单位混合种族+建筑+弹道+特效+菌毯对局 ≥ 60fps；非批量单位节点数压到每单位 ≤ 5。

**进度**：
- [x] 混编压测场景 `StressTestMixed`（2000 单位，20 种兵种循环，确定性生成，支持回放录制/校验）。
- [x] SIM_PROFILE 性能探针（SimManager 系统级 + SimWorld 内部级，纯 C#）。
- [x] 探针结论：600 混编单位下逻辑 CPU 约 7ms/tick（MovementIntent≈2ms、EntityCollisions≈1.8ms、DemonAuras/Fields≈0.4ms），逻辑侧远未饱和；混编墙钟慢来自模拟线程等待主线程事件屏障（渲染/特效拖累）。
- [ ] 渲染/节点压力缓解（按用户指示暂缓，等人工建模替换后重测）。

### P1-3 特效/节点 GC 尖峰

特效每帧创建销毁，目前只是用距离+抽样降级掩盖，没有对象池。

**验收**：满载对局 GC 尖峰不造成可见卡顿；特效全部走对象池。

**进度**：
- [x] 弹道光束/枪口闪光对象池（复用节点+Mesh+材质，上限 256/128）；2000 单位回放回归 PASS。
- [x] 命中光球/扩散圈/扇形喷吐等其余特效入池（HitFx.HitPool/ConePool，重战斗不再 GC 卡顿）。

### P1-4 对局控制

暂停、投降、协商、投票、掉线后的机器人接管都没有。

**验收**：以上功能可用且进锁步指令流。

**进度**：
- [x] 本地暂停（P 键，锁步 tick 边界生效）。
- [x] 投降 + 胜负判定：`Surrender` 锁步指令（K 键），投降方单位/建筑确定性清除，仅剩一队时判胜停模拟；录制→重放 5/5 哈希一致 PASS。
- [x] 掉线机器人接管（锁步踢人后自动索敌）。
- [x] 协商/投票：锁步 `Vote` 指令（`VoteRematch` / `VoteSurrenderTeam`），全票同意生效、超时自动作废，状态纳入区段哈希；Ctrl+H 发起重开投票、N/M 表决，`VoteUI` HUD 展示与结果提示。验收：构建 PASS + SIM_THREAD_CHECK_PASS + Arena 600 帧确定性 PASS。
- [ ] 协商（可选项：队内投降 UI 按钮入口）。

### P1-5 社交层

聊天、信号 ping、表情——联机 RTS 的基本盘。

**验收**：对局内可聊天、可 ping 地图、可发表情。

**进度**：
- [x] 聊天数据链路：锁步 `Chat` 动作（T 键测试消息）+ 双端消息日志。
- [x] 聊天 UI 面板：Enter/T 打开输入框、回车发送、Esc 关闭；聊天输入打开时屏蔽暂停/投降/信号快捷键；历史记录来自锁步快照（`ChatLock` 保护，UI 主线程只读）。
- [x] 地图 ping：中键发锁步 `Ping` 指令（TargetX/Y 与 Move 同编码），全端渲染玩家颜色标记，3 秒过期；渲染层去重防重复标记。
- [ ] 表情。

### P1-6 旁观者 / 大厅机器人 / 队伍分组

**验收**：可旁观（全图、不可操控、看所有人资源）；单机大厅可逐机器人添加并选种族/出生点/难度；支持 2v2 队伍分组。

**进度**：
- [x] 旁观者：大厅“旁观者模式”开关 → 不生成玩家实体、FogOfWar 全图、UserController 输入屏蔽、右上角旁观者面板显示所有队伍资源/人口；锁步清单带 `obs` 标记双端一致。
- [x] 逐机器人：大厅“添加机器人”逐行配置（种族/出生点/难度/队伍），覆盖旧的“数量+统一难度”；每个机器人独立难度（`SetBotDifficulty` 按队伍覆盖资源倍率）。
- [x] 队伍分组（2v2）：清单 `pid:team:race:group:obs`；同组 = 盟友（自动索敌/右键指令/机器人索敌/胜利判定全部按阵营组）；盟友共享视野（FogOfWar.CurrentViewerTeams）；出生点仍唯一（每名玩家有自己的基地与资源，标准 RTS 组队）。
- [x] 自动化验证：`AI_TEST_TEAMS/GROUPS/RACES/BOTS/OBSERVER` 环境变量驱动 2v2 四人局与旁观者局，无头回归通过。

## P2 — 决定“品质上限”

### P2-1 地图编辑器/导入校验

地图是手摆的，没有编辑器、没有地图合法性校验。

**验收**：可编辑并导出地图；非法地图（重叠、出界、缺出生点）导入被拒并给出原因。

### P2-2 配置表加载期校验

.tres/JSON 手工维护，缺字段、类型错、字符串引用（TechId/UnitTypeId）只在运行时炸。

**验收**：加载期校验器拦截缺字段/类型错/悬空引用；CI 跑全表校验。

**进度**：
- [x] `ConfigDatabase.ValidateAll`：单位/建筑/科技/武器/Buff 的悬空引用加载期拦截（T1~T4 等级门白名单）；实测启动即拦截了 6 处历史脏引用。
- [x] 语义校验：负成本/零血/零移速/零长宽/生产队列未配置/AI 计划悬空引用（AiBuildOrderIds/AiUnitMixIds/AiTechOrderIds/AiWorkerUnitId/AiBuildTargetCounts）在启动时拦截；五族全表实测通过。
- [x] CI 挂接：`run_ci.ps1` 启动游戏即触发 `LoadAll → ValidateAll`，校验失败直接抛异常。

### P2-3 美术/音频管线

占位模型、动画只有 Idle/Run、VAT 烘焙靠手动跑工具、无特效规范、音乐音效零星。

**验收**：有统一的模型/特效/音频规范与一键烘焙工具。

### P2-4 新手引导/教学/战役

缺失。

**验收**：有新手教程与至少一个战役关。

### P2-5 设置页

分辨率/画质/键位绑定/语言都是写死的（改视角靠改代码）。

**验收**：设置页可改分辨率、画质、键位、语言并持久化。

**进度**：
- [x] 设置菜单（主菜单“设置”按钮 / `OPEN_SETTINGS=1` 调试）：语言（zh_CN/en_US）、键位重绑（P/K/T → InputMap 动作）、主/音乐/音效音量、全屏开关。
- [x] 持久化：`user://settings.cfg`（`GameSettings.Load/Save`，启动自动应用）。
- [ ] 分辨率/画质选项（当前只做了全屏）。

### P2-6 本地化框架

UI 全中文硬编码。

**验收**：所有 UI 文案走本地化表，可切换语言。

**进度**：
- [x] 本地化框架（`Localization.Tr` + zh/en 表），设置菜单与主菜单按钮已接入。
- [x] 现代游戏常用语言列表（zh/en/ja/ko/de/fr/es/pt/ru/it），CSV 配置表（`Localization/<code>.csv`），回退链 当前语言→en_US→key。
- [x] 内容名称本地化：全部阵营单位/建筑 `name.*` 键（zh/en），已接入选中信息面板与动作按钮（`action.*` 键）。
- [ ] 其余硬编码 UI（大厅/房间/动作详情/提示）迁移到 Tr。

### P2-7 统计/天梯/战绩

MMR、段位、赛后数据都没有。

**验收**：赛后统计落盘，天梯分可查。

### P2-8 存档/云存档

单机进度、设置、回放都没有落盘体系。

**验收**：设置/进度/回放可保存、可恢复、可云同步。

## P3 — 长期债务

### P3-1 工程卫生

SimManager 2000+ 行、注释乱码（GBK/UTF-8 混）、遗留调试输出、命名/缩进混乱（“else 挂错层级”就是这类问题的直接后果）。

**验收**：SimManager 拆分到可读规模；注释/命名统一；删除遗留调试输出。

### P3-2 CI/单元测试

除 sim 对拍外没有自动化回归。

**验收**：PR 自动跑编译+单测+确定性对拍。

**进度**：
- [x] `Tools/run_ci.ps1` 本地 CI 骨架（编译 + Arena 确定性自测）。
- [ ] 挂入 Git 钩子/外部 CI。

### P3-3 导出自检

出过“配置表漏打包”事故。

**验收**：导出后自动校验配置/资源完整性。

### P3-4 崩溃上报/日志收集

缺失。

**验收**：崩溃自动生成报告并可上传。

### P3-5 平台单一

GodotSteam 绑定死，无 LAN/自定义服务器；Godot 4.5.1 版本锁定，升级成本高。

**验收**：至少支持 LAN 对战；平台层抽象可插拔。

---

## 执行顺序

P0 逐条落地，每完成一条更新本文件状态并跑对应验收。
