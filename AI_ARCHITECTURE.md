# 七族 AI

目标是简单、可靠、按种族机制定制。使用固定优先级和少量状态，不引入搜索树、学习模型或按难度切换的微操系统。设计依据为 `设计/设计数据表 (version 1).xlsb.xlsx`；具体成本、范围、科技前置以当前 `Data/Configs` 和实际动作实现为准。

## 调度与职责

`SimManager.BotAI.Core.cs` 每 30 个模拟 tick（1.5 秒）按队伍错峰调用策略。`SimManager.BotAI.Strategies.cs` 注册七个无状态策略；每队的波次、补弹、蓝图与编队状态保存在 `BotBrain`，开局重置。新增种族必须注册自己的策略。

`Core` 提供采集、建造、生产、研究等执行工具，`Combat` 提供局部索敌、波次和拆塔工具。每族单独的 partial 文件决定调用哪些工具、执行顺序和限制。多足沿用现有编队系统作为唯一的宏观指挥者。

| 种族 | 定制行为 |
| --- | --- |
| 联盟 Union | SCV 经济、人口建筑、付费混编生产；交战且血量足够时使用兴奋剂。 |
| 泰伦 Terran | 驻扎藻厂提供食物；弹药低于 30% 返回补给，补至 80% 再归队；波次及拆塔不覆盖补弹、架设任务。 |
| 恶魔 Demon | 使用绑定怨灵和献祭建造，保留基本经济怨灵；裂隙自动出兵并切换模式；推进与拆塔受立场覆盖约束。 |
| 纳米 Nano | 无工人和常规造兵；铺毯收入、面板建筑、前沿炮塔；保护唯一巨兽，低血量撤回，未研究移动科技时检查路径上的己方菌毯。 |
| 植物 Plant | 森林即经济；从前沿可用节点继续扩林；无工人面板建造；出兵保留 50 木扩林预算，存量达到 100 木才扩林，人口紧张先攒分支树；遇袭可动用扩林预留款。 |
| 洞穴 Cave | 一次派一个工人重建残骸，保护重建任务；民兵科技后，仅用附近空闲富余工人驻防。 |
| 多足 Wanderer | 独立游牧经济、编队生产和分线指挥；先开矿或清塔，再交替建立经济与攻击队，支持迁移和阵亡恢复。详见 [WANDERER_AI.md](./WANDERER_AI.md)。 |

研究通过真实动作检查前置与互斥项，再进入玩家研究状态；不可用科技不阻塞后续可用项。生产只选择真实可执行单位，成功入队才更新轮换状态，工人目标包括所有生产队列。

## 难度

所有难度使用同一策略与决策间隔。`SimManager.GetBotResourceMultiplier` 保持现有资源收益倍率：简单 0.7、普通 1.0、困难 1.5；非 AI 收益不受影响。后续调整难度优先修改此倍率，不复制七套策略、不改变单位基础数值。

## 验证和边界

`Scenes/Test/RaceAISmokeTest.tscn` 使用真实 Godot 实体、动作和配置，覆盖七族策略入口、错峰周期、资源倍率、研究、互斥科技、弹药任务、立场与菌毯约束、波次持续下令、植物扩林和人口、洞穴重建、生产。已接入 `Tools/run_ci.ps1`；线程检查脚本包含全部 AI partial 文件。

整局开局验证使用 `Scenes/Test/AIShowcase1v1.tscn`，设置 `AI_TEST_BOTS=1,2`、`AI_TEST_RACE1`、`AI_TEST_RACE2`、`AI_TEST_SPEED=8`、`AI_TEST_TICKS=1200`。联盟/恶魔、泰伦/植物、纳米/洞穴、多足/联盟均已运行超过 1200 tick。日志在 `tmp/ai_refactor_*.log`，属于本地验证输出。

独立模拟确定性测试不包含 Godot AI 动作，不代表完成了双端联网 AI 确定性验证。AI 状态已加入世界分区哈希，便于后续定位分歧；此前记录的旧版回放哈希不能直接当作新版基线。

最终代码通过 `Tools/run_ci.ps1`（Debug/Release 模拟测试、两个 Godot 冒烟场景、线程扫描及编译）。植物资金阈值调整后，泰伦/植物再次运行至 tick 3603，植物继续出兵并产生新的森林节点；此结果只证明开局循环运行，不代表双方经济或战力已经平衡。

设计表中的洞穴驻扎采集和部分分类增益尚未完整实现，本次仅调用当前可用的重建和驻防动作。Godot headless 测试退出时仍有渲染资源释放告警；这些退出告警与 AI 断言结果分开记录。开局回归不代表所有地图、长期对战与胜率平衡已经验证。
