# 多足 AI：游牧经济与多线进攻

本轮按确认的方向重写：快速扩张建立经济，然后多线进攻。保留牧羊人带队、成员归属和蓝图完成后入队的机制；重写编队建立、经济分配、生产、迁移、目标选择和恢复规则。此前的牧羊人友军推挤免疫继续保留。

## 代码职责

| 文件 | 职责 |
| --- | --- |
| `Scripts/Core/SimManager.BotAI.Wanderer.cs` | 策略入口、编队生命周期、出生归属、施工中断恢复、诊断 |
| `Scripts/Core/SimManager.BotAI.Wanderer.Production.cs` | 建造派工、订单预留、经济队与战斗队建立、按缺口补兵 |
| `Scripts/Core/SimManager.BotAI.Wanderer.Orders.cs` | 矿点选择、编队内金属/气分配、游牧迁移、目标分线、控制圈跟随与撤退 |

多足不再调用共享的全局经济、波次战斗与普通生产调度。研究仍使用真实科技动作与玩家研究状态，作为发育后的剩余资源支出。难度继续使用现有资源收益倍率，未修改单位战斗数值、生产成本或起始资源。

## 固定决策规则

1. 初始牧羊人带出生工人组成经济队，优先补至 4 名建造者。每队自己的工人分配金属与气，避免全局采集调度把工人拉往其他矿区。
2. 有安全资源点时优先第二个经济队；所有矿点有塔时，先建立攻击队清塔。之后经济队与攻击队交替建立，默认最多 4 个经济队和 3 个普通攻击队。人口不足时允许补额外牧羊编队。
3. 经济队枯矿后寻找未占安全矿点。迁移明确中断旧采矿命令，成员归属保持不变；牧羊人按完整终点寻路，并在成员掉队时暂停等待，成员跟随其当前位置。到达后恢复本队采集。
4. 攻击队有自己的建造者，普通编制目标为 3 游猎、1 战车、3 收割。收割者是战斗单位。优先保留战车费用，不用廉价填充兵挤掉前排位置。初次具备 5 个战斗单位即可出击，减员至 2 个仍可维持作战。
5. 多支攻击队优先选择不同敌对据点；目标在短期内保持稳定，目标死亡或到期后再选择。早期优先打开中立矿点，牧羊人留在控制圈后方，成员向目标作战。
6. 补员不召回已经具备战力的军队。牧羊人低于半血时向经济队驻点撤退，工人保留其正常修复机制。
7. 两个经济队与两个攻击队建立且资源充裕后，允许一支牧羊人带领的电浆炮队；不再创建无限空的超级编队。

## 可靠性约束

每队同时最多一个待完成蓝图订单。订单及派工工人在主线程生成蓝图之前就登记，避免跨模拟帧重复下单；蓝图完成后按原队归属入队。调度可借用其他队的空闲建造者施工，但不改变其成员归属。

工人死亡或施工被打断时，给现有蓝图重新派工；队长死亡时保留成员，清理阻挡重建的未完成战斗蓝图并优先重建牧羊人。取消/失败订单可以重试。无归属幸存单位才允许重新收编，正常经济工人不会被转入攻击队。

新订单、派工者、任务目标和推进状态加入 AI 分区哈希。决策仍每 30 tick 错峰执行，使用固定优先级和稳定 ID 排序，没有搜索树或按难度切换的微操算法。

## 验证

`RaceAISmokeTest.Wanderer.cs` 在真实 Godot 实体与动作上验证初始编队、金属/气采集、安全矿选择、迁移中断旧采矿、真实造兵、即时订单去重、完成入队、队长死亡保留成员、分线目标，以及补员不召回军队。原牧羊人碰撞测试和其他种族回归继续运行。

普通小地图使用 `Scenes/Test/AIShowcase1v1.tscn`，大地图使用新增的 `Scenes/Test/AIShowcaseLarge.tscn`。`AI_TEST_CAMERA_SIZE` 只改变视距，不能作为大地图测试替代品。

诊断日志每 300 tick 输出队伍种类、牧羊位置、驻点、工人及实际采矿人数、战斗人数、待生产单位和目标 ID。通过这些数据核对完整发育链，而不只看人口增长。测试日志为 `tmp/wanderer_*.log`。

本轮验证着重消除发育和指挥故障；强度排名仍需要多地图、多种族对局统计。独立模拟测试也不替代双端联网 AI 确定性验证。

### 最终版本结果

- `tmp/wanderer_verified_ci.log`：主项目零警告、零错误，线程扫描、Debug/Release 模拟确定性、两个 Godot 冒烟场景通过，输出 `CI_PASS`。
- `tmp/wanderer_verified_large.log`：真实大地图，多足对泰伦，普通难度，运行到 tick 12010。末次状态为 74/125 人口、36 单位；两个经济队各 4 名工人实际采矿，三支攻击队分别指向不同目标。第二经济队完成从出生区到新矿区的迁移。
- `tmp/wanderer_verified_small.log`：小地图多足对联盟运行到 tick 9000，受压减员后仍有 22/50 人口、8 单位及建造者蓝图在施工。此样本表明仍需评估小地图承压能力，不能据此宣称多足已达到最强胜率。

Godot headless 退出时仍有既有渲染资源释放告警，和 AI 断言及运行中异常分开记录。
